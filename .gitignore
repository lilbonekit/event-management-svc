# === Go build artifacts ===
/bin/
/tmp/
/dist/
/build/
/coverage.out
*.exe
*.test
*.out

# === Go modules ===
go.work
go.work.sum

# === Environment / secrets ===
.env

# === SQLite / local databases ===
*.db

# === IDE / Editor stuff ===
.vscode/
.idea/
.DS_Store

# === Swagger auto-generated files ===
# swagger.json / swagger.yaml — можно оставить если хочешь документацию версионировать,
# но чаще это просто артефакт. Лучше исключить.
docs/swagger.json
docs/swagger.yaml

# docs.go — наоборот, лучше оставить, если не хочешь каждый раз генерировать
# (но можно и исключить, если CI будет делать `swag init`)
# docs/docs.go

# === Air hot reload ===
.air.toml
/tmp/
